# Build artifacts - runtime files (template.json is embedded and copied at runtime)
app/build/bin/*.exe
app/build/bin/*.dll
app/build/bin/config.json
app/build/bin/user_settings.json
app/build/bin/user_settings_*.json
app/build/bin/cache.db
app/build/bin/vpn.log
app/build/bin/sing-box.exe
app/build/bin/template.json
app/build/bin/bin/
app/build/bin/profiles.json
app/build/bin/app_config.json
app/build/bin/resources/

# Release builds
release/
/build/

# Local development documentation
/docs/

# Executable dependencies remain a local verified build cache. The reviewed
# routing catalogs are the one exception: they are versioned in GitHub and
# embedded into every build so clients never download them at runtime.
dependencies/*
!dependencies/metacubex-utls-LICENSE.txt
!dependencies/filters/
dependencies/filters/*
!dependencies/filters/*.srs
!dependencies/filters/*.lst
!dependencies/filters/version.json
!dependencies/filters/LICENSE.Re-filter-lists.txt

# Go
*.so
*.dylib
*.test
*.out
go.work
go.work.sum

# Python
__pycache__/
*.py[cod]

# Node.js
app/frontend/node_modules/
app/frontend/dist/
app/frontend/playwright-report/
app/frontend/test-results/
app/frontend/package.json.md5
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Wails
app/frontend/src/wailsjs/

# IDE
.idea/
.vscode/*
!.vscode/settings.json
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Environment
.env
.env.local
.env.*.local
*.local

# Secrets (никогда не коммитить!)
**/private_key*
**/secret*
**/*credentials*
**/*password*
**/*.p12
**/*.pfx
**/*.keystore
**/*.jks
**/*signing.properties
# Subscription / proxy material accidentally dumped to disk
**/sub.txt
**/subscription*.txt
**/*.subscription
# Client DPI fingerprints captured at runtime (resources copy; lab keeps its own DB)
app/resources/fingerprints/
resources/fingerprints/

# Build output left next to sources
app/*.exe
app/dropo.exe
flutter_app/android/app/libs/*.aar
flutter_app/android/app/libs/*.jar
flutter_app/android/app/libs/*.version

# Production release publisher — owner-only, must NOT be public
release.ps1
release.local.ps1

# Test-lab generated artifacts (regenerated by tools/build_profile.py)
testlab/profiles/censor-profile.json

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Desktop.ini

# Bridge auth token (generated at runtime next to dropo-core, never committed)
bridge-token
app/bridge-token

# Temporary files
tmp/
*.tmp
*.temp
*.bak
*.backup

# Test coverage
coverage/
*.cover
*.coverage

# Debug
debug/
*.debug
bootstrap/payload.zip

# Local build SDKs installed for development only
.toolchain/
